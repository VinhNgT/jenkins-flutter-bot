[project]
name = "config-schema"
version = "0.1.2-dev.3"
description = "Declarative configuration schema framework for the Jenkins Flutter Bot stack"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
