# ==============================================================================
# Jenkins Flutter Bot Stack — Docker Compose Environment
# ==============================================================================
# Application configuration is managed through the config-ui dashboard.
# Populate bot and agent settings in the web dashboard after the stack starts.
#
# For production deployments without config-ui, use the "Export .env" tool
# in the config-ui dashboard (Tools → Export .env) to generate this file
# automatically from your saved configuration.
#
# These env vars are only needed if you want to override docker-compose defaults
# or run containers outside of compose.
#
# ---- Jenkins ----
# The bundled jenkins service is a development/testing convenience.
# In production, point JENKINS_URL to an external Jenkins instance
# and remove the jenkins service from docker-compose.yml.
#
# ---- Agent (flutter-agent) ----
# JENKINS_URL=http://jenkins:8080
# JENKINS_AGENT_NAME=flutter-agent
# JENKINS_SECRET=<your-agent-secret>        # Required — get from Jenkins node config
# JENKINS_WEB_SOCKET=true                   # Use WebSocket (default) or set false for TCP
# JENKINS_TUNNEL=                           # Only needed with WEB_SOCKET=false
#
# ---- Bot (tg-bot) ----
# BOT_SERVICE_URL=http://tg-bot:9090         # Internal URL (port is derived automatically)
# CONFIG_PATH=/config/bot/bot.json
# MAX_RECENT_BUILDS=0                      # Max bot-triggered builds kept (0 = unlimited)
#                                           # Config key: bot.max_recent_builds
